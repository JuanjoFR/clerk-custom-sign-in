# Clerk environment variables for local development
# Get your keys from https://dashboard.clerk.com/
#
# Never commit real secrets to version control. This file is safe to commit as an example.

# Public Clerk publishable key (safe to expose to the browser)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_1234567890abcdef

# Secret Clerk backend key (keep this private in real .env files)
CLERK_SECRET_KEY=sk_test_abcdef1234567890

# Custom sign-in page URL (relative to your app, e.g. /sign-in)
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in

# Where to redirect after sign-in if no other redirect is set
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/

# Where to redirect after sign-up if no other redirect is set
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/welcome
